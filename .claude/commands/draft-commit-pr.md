---
description: Draft a Conventional Commit message, PR title, and PR body from staged changes (text only, does not commit or push)
---

Optional hint: $ARGUMENTS

Read `.ai/commands/draft-commit-pr.md` and follow it exactly. If a hint is provided, use it to bias the type/scope/description. Output text only — do NOT run `git commit`, `git push`, or create a PR.
